1. Run build & tests and confirm everything works
2. Ensure there test coverage for the changes on the critical paths. If tests are added, re-test before continuing.
3. Run `/capture-evidence` to collect visual proof (optional, skip if no TUI changes)
4. Commit and push changes (including any evidence files)
5. Create a PR with evidence section in body
6. Wait until checks pass
